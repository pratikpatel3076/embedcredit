{
  "name": "vantage-credit-server",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test_credit_subsystem.js && node test_drawdown_repayment.js",
    "seed": "node -e \"require('dotenv').config(); require('./config/db').connectDB().then(function () { return require('./seed').seedDatabase(true); }).then(function () { console.log('[seed] done'); process.exit(0); }).catch(function (e) { console.error(e); process.exit(1); });\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.1",
    "vantage-credit": "file:.."
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0",
    "nodemon": "^3.1.4"
  }
}
